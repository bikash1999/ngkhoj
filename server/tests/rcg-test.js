var chai = require("chai"),
  Promise = require("promise");
  
describe('ngpedia Test', function() {
    describe('Create a ngpedia', function() {
        var newRcgId;
        var promise;
        it('should create a ngpedia', function(done) {                 
            done();      
        });
    });
});